# Core data stack
pandas>=2.0
numpy>=1.24
scipy>=1.10

# Visualisation
matplotlib>=3.7
seaborn>=0.12
adjustText>=1.0          # repositionnement automatique des labels (matrice d'opportunité)

# Analytics & ML
duckdb>=0.9              # requêtes SQL analytiques in-process
scikit-learn>=1.3        # DBSCAN, clustering hiérarchique

# Géospatial
folium>=0.15            # cartes interactives
openlocationcode>=1.0    # décodage des Google Plus Codes

# Ingestion
requests>=2.31           # téléchargement du dataset (Google Sheet)

# Environnement notebook
jupyter>=1.0
notebook>=7.0
